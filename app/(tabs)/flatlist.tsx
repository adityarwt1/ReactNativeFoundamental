import React from "react";
import { StyleSheet, Text, View } from "react-native";

const flatlist = () => {
  return (
    <View>
      <Text>flatlist</Text>
    </View>
  );
};
/// flatlist cdoe attach3ed to the asset/tutorila/flatlist
export default flatlist;

const styles = StyleSheet.create({});
